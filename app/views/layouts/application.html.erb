<!DOCTYPE html>
<html>
  <head>
    <title>るせっと</title>
    <!--<link href="<%#= image_path("favicon.ico") %>" rel="shortcut icon" type="image/x-icon" />-->
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="sticky-top">
      <nav class="navbar navbar-expand-md navbar-light mt-0 mb-0" style="background-color: #CCF7FA; padding: 10px;">
        <a class="navbar-brand" href="/">
          <%= image_tag 'RecetteLogo.jpg', width: '80px', height: '80px' %>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="container navbar-nav ml-auto" style="justify-content: flex-end;">
            <% if worker_signed_in? %>
            <li class="nav-item active">
                <%= link_to "レシピ登録", new_worker_recipe_path, class: "nav-link" %>
              </li>
              <li class="nav-item active">
                <%= link_to "レシピ一覧", worker_recipes_path, class: "nav-link" %>
              </li>
              <li class="nav-item active">
                <%= link_to "商品案一覧", worker_ideas_path,method: :get, class: "nav-link" %>
              </li>
              <li class="nav-item active">
                <%= link_to "ログアウト", destroy_worker_session_path, method: :delete, class: "nav-link" %>
              </li>
            <% elsif admin_signed_in? %>
              <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: "nav-link" %>
            <% end %>
          </ul>
        </div>
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
